Source: https://www.odoo.com/documentation/19.0/applications/essentials/property_fields.html

# Property fields

Property fields, or properties, enable the customization of a form view by adding various field types. These fields allow information storage and management by adding values.

Property vs. regular fields

Properties act as pseudo-fields; they behave like regular fields but are not saved as columns in the database. They also rely on a defined parent record.

Example

Adding a property to a _task_ inserts a field in _all tasks_ within the _same project_ while other projects’ tasks remain unaffected.

## Add property fields

To add a first property field to a form view, click the __( Actions) icon, then select __ Edit Properties.

In the popover, enter the property’s Label, choose a Field Type, and then configure the field based on the selected type:

Field Type,Use,Options
Text,Short text on a single line,Enter a Default Value if desired.
Multiline Text,Full text on multiple lines,Enter a Default Value if desired.
HTML,HTML field,Enter a Default Value if desired.
Checkbox,Checked or unchecked status,Choose the Default State.
Integer,"Integer numbers (positive, negative, or zero, without a decimal)",Enter a Default Value if desired.
Decimal,"Decimal numbers (positive, negative, or zero, with a decimal)",Enter a Default Value if desired.
Date,Selection of a date on a calendar,Select a Default Value if desired.
Date & Time,Selection of a date on a calendar and a time on a clock,Select a Default Value if desired.
Selection,Selection of a value from a group of predefined values,"Add a selectable option by clicking ⨁ Add a Value and entering the Option Name. Set an option as default by clicking the ⋆ button. Reorder options by dragging using ≡ handle. Delete an option by clicking ⨁ Remove Property."
Tags,Selection of multiple values in the form of tags,"Enter a Tag name and press Enter to save it. Change a tag's color by clicking it and selecting another one."
Many2one,Selection of a single record from another model,"Enter the Model name. Configure its Domain to filter records if needed. Select a Default Value if desired."
Many2many,Selection of multiple records from another model,"Enter the Model name. Configure its Domain to filter records if needed. Select a Default Value if desired."
Separator,Group several properties under a foldable label,N/A


Click outside the popover to save the added property.

Note

  * Select whether to display the property in the Kanban, List, or Calendar views cards for every field with the Display in Cards option.

  * To add another property, click __ Add a Property at the bottom of the form while.

Tip

To edit an existing property, hover the cursor over the property:

  * Click the __( pencil) button to open a popover and modify the property. In the popover, click the __(up) or __(down) chevron to move a property upwards or downwards.

  * Click __ Delete, then Delete to delete it. Deleting a property is permanent.

  * Use the __( drag handle) icon to drag and drop the property to reorder or regroup.

## Properties across apps

Property fields can be defined in the form view of multiple models. Once set, the property is shared by all records that are linked to the same _parent_.

App,Model,Parent
Accounting,Asset/Revenue Recognition,Asset model
Accounting,Loan,Journal
Appraisals,Employee Appraisal,Department
Approvals,Approval Request,Category
CRM,Lead/Opportunity,Sales team
Employees,Employee,Company
Events,Event Registration,Event
Fleet,Vehicle,Vehicle model
Frontdesk,Frontdesk Visitors,Station
Helpdesk,Ticket,Helpdesk team
Inventory,Lot/Serial,Product variant
Inventory,Transfer,Operation type
Inventory,Batch Transfer,Operation type
Knowledge,Knowledge Article,Parent article
Maintenance,Maintenance Equipment,Equipment category
Meeting Rooms,Room,Office
Planning,Shift,Role
Project / Field Service,Task,Project
Recruitment,Applicant,Job position
Recruitment,Job Position,Company
Recruitment,Candidate,Company
Repairs,Repair order,Company
Sales / etc.,Product,Category

Note
The following models do not depend on any _parent_ and apply to all records:

  * Contact Form in the Contacts app

  * Mailing List Contacts in the Email Marketing app
